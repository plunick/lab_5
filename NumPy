import numpy as np
# импортируем модуль NumPy и присваиваем ему псевдоним np, чтобы обращаться к его функциям и классам.

# Задание 1
array1 = np.arange(1, 11)
print("Задание 1:")
print(array1)
  # Создаем одномерный массив при помощи функции arange из модуля NumPy. Функция arange возвращает значения 
  # от 1 до 10 (включительно) и сохраняет их в переменную array1. Затем мы выводим этот массив на экран с помощью функции print

# Задание 2
array2 = np.random.rand(3, 3)
print("\nЗадание 2:")
print(array2)
  # Создаем двумерный массив размером 3x3, состоящий из случайных чисел от 0 до 1, с помощью функции random.rand из модуля NumPy

# Задание 3
array3 = np.zeros(10)
array3[2] = 5
print("\nЗадание 3:")
print(array3)
  # Создаем одномерный массив размером 10, заполненный нулями, с помощью функции zeros из модуля NumPy. 
  # Изменяем значение третьего элемента массива на 5, обращаясь к нему по индексу

# Задание 4
array4 = np.arange(1, 13).reshape(3, 4)
print("\nЗадание 4:")
print(array4)
  # Создаем одномерный массив, содержащий числа от 1 до 12, с помощью функции arange из модуля NumPy. 
  # Преобразуем этот массив в двумерный массив размером 3x4, используя метод reshape

# Задание 5
array5 = np.random.randint(0, 10, size=(5, 5))
sum5 = np.sum(array5)
print("\nЗадание 5:")
print(array5)
print(f"Сумма элементов массива: {sum5}")
  # Создаем двумерный массив размером 5x5, заполненный случайными числами от 0 до 10, с помощью функции random.randint из модуля NumPy. 
  # Суммируем все элементы этого массива при помощи функции sum из модуля NumPy и сохраняем результат в переменную sum5. 
  # Выводим массив и сумму его элементов на экран.

# Задание 6
array6 = np.random.randint(1, 11, size=(4, 4))
max_values = np.max(array6, axis=1)
print("\nЗадание 6:")
print(array6)
print("Максимальные значения в каждой строке:")
print(max_values)
  # Создаем двумерный массив размером 4x4, заполненный случайными числами от 0 до 1, с помощью функции random.rand. 
  # С помощью функции max вычисляем максимальные значения в каждой строке массива и сохраняем их в переменную max_values. 
  # Умножаем каждый элемент массива на 2 с помощью оператора *=, изменяя значения в самом массиве

# Задание 7
array7 = np.random.rand(3, 3)
result7 = array7 * 2
print("\nЗадание 7:")
print(array7)
print("Результат умножения на 2:")
print(result7)
  # Создаем два двумерных массива размером 3x3, заполненных случайными числами от 0 до 1, с помощью функции random.rand. 
  # С помощью функции dot вычисляем матричное произведение этих двух массивов и сохраняем результат в переменную result

# Задание 8
array8_1 = np.arange(9).reshape(3, 3)
array8_2 = np.arange(9).reshape(3, 3)
result8 = np.dot(array8_1, array8_2)
print("\nЗадание 8:")
print(array8_1)
print(array8_2)
print("Результат матричного произведения:")
print(result8)
  # Cоздаем трехмерный массив размером 3x3x3, заполненный случайными числами от 0 до 1, с помощью функции random.rand. 
  # C помощью функции linalg.det вычисляем определитель этого трехмерного массива и сохраняем результат в переменную det.

# Задание 9
array9 = np.random.randint(0, 6, size=(3, 3))
determinant = np.linalg.det(array9)
print("\nЗадание 9:")
print(array9)
print(f"Определитель матрицы: {determinant}")
  # создаем двумерный массив размером 3x3, заполненный случайными целыми числами от 1 до 10 с помощью функции random.randint. 
  # C помощью функций max, min и mean вычисляем максимальное, минимальное и среднее значения в этом массиве и сохраняем их в соответствующие переменные max_value, min_value и mean_value
